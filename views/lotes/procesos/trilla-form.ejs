<div class="container mt-5">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/fincas/gestionar">Mis Fincas</a></li>
            <li class="breadcrumb-item"><a href="/fincas/<%= finca.id %>/lotes">Lotes de: <%= finca.nombre %></a></li>
            <li class="breadcrumb-item"><a href="/fincas/<%= finca.id %>/lotes/<%= lote.id %>/procesos">Procesos Lote: <%= lote.codigo %></a></li>
            <li class="breadcrumb-item active" aria-current="page"><%= titulo %></li>
        </ol>
    </nav>

    <h1 class="text-toastem-primary mb-4"><%= titulo %></h1>

    <%# Mostrar mensajes flash %>
    <% if (mensaje && mensaje.length > 0) { %>
        <div class="alert alert-success alert-dismissible fade show" role="alert"><%= mensaje %><button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button></div>
    <% } %>
    <% if (error && error.length > 0) { %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <strong>Error:</strong>
            <ul><% error.forEach(function(err){ %><li><%= Array.isArray(err) ? err.join(', ') : err %></li><% }) %></ul>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    <% } %>

    <div class="card shadow-sm">
        <div class="card-body">
            <p class="mb-3"><strong>Peso del lote después de Clasificación (Peso inicial para trilla):</strong> <%= peso_clasificado_final %> kg</p>
            <hr/>
            <form action="/fincas/<%= finca.id %>/lotes/<%= lote.id %>/trilla" method="POST">
                
                <div class="mb-3">
                    <label for="fecha_trilla" class="form-label">Fecha de Trilla: <span class="text-danger">*</span></label>
                    <input type="date" class="form-control" id="fecha_trilla" name="fecha_trilla" value="<%= fecha_trilla %>" required>
                    <small class="form-text text-muted">Campo obligatorio. Fecha en que se realizó la trilla.</small>
                </div>

                <div class="mb-3">
                    <label for="peso_final" class="form-label">Peso Final (kg): <span class="text-danger">*</span></label>
                    <input type="number" step="0.01" class="form-control" id="peso_final" name="peso_final" value="<%= peso_final %>" placeholder="Ej: 20.50" required>
                    <small class="form-text text-muted">Campo obligatorio. Peso del café después del proceso de trilla (café verde o "almendra").</small>
                </div>

                <div class="mb-3">
                    <label for="observaciones" class="form-label">Observaciones:</label>
                    <textarea class="form-control" id="observaciones" name="observaciones" rows="3" placeholder="Anotaciones sobre el proceso de trilla"><%= observaciones %></textarea>
                </div>
                
                <div class="d-flex justify-content-end gap-2 mt-4">
                    <a href="/fincas/<%= finca.id %>/lotes/<%= lote.id %>/procesos" class="btn btn-secondary">Cancelar</a>
                    <button type="submit" class="btn btn-primary">Registrar Trilla</button>
                </div>
            </form>
        </div>
    </div>
</div>

<style>
    .text-toastem-primary {
        color: #5D4037; /* Color principal de Toastem */
    }
</style> 